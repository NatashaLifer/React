export enum COMMON_ROUTES {
    HOME = '/',
    LOGIN = '/login',
    REGISTER = '/register', 
    PRODUCTS = '/products', 
    NOT_FOUND = '/404',
}

export enum USER_ROUTES {
    PRODUCT = '/products/:id', 
}

export enum ADMIN_ROUTES {
    USERS = '/users', 
    SESSIONS = '/sessions',
}